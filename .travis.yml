language:
    - c
compiler:
    - gcc
notifications:
      irc: "chat.freenode.net#portalmod"
before_script:
    - sudo apt-get update -qq
    - sudo apt-get install -y python-nose libjack-jackd2-dev libjack-jackda2-0 jackd2
    - bash .create_lv2_env.sh
    - make init_tests
script:
    - nosetests
after_script:
    - make end_tests
